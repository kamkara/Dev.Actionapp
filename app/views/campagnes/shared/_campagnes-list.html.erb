<% @title="Liste des campagnes" %>
<!-- component -->

<section class="h-screen w-screen bg-gradient-to-br from-pink-50 to-indigo-100 p-8">
    <h1 class="text-center font-bold text-2xl text-indigo-500">Liste des campagnes </h1>

    <div class="grid justify-center md:grid-cols-2 lg:grid-cols-3 gap-5 lg:gap-7 my-10">
        <!-- Card 1 -->
        <% @CampagnesList.each do |camp| %>
            <div class="bg-white rounded-lg border shadow-md max-w-xs md:max-w-none overflow-hidden">
               <% if camp.heroImg.attached? %>
                    <%= image_tag(camp.heroImg, class:"w-full") %>
                <% else %>
                <p class="">Pas d(image de couverture</p>
               <% end %>
            
        
                <div class="p-3">
                    <span class="text-sm text-primary"><%= camp.start_date %></span>
                    <h3 class="font-semibold text-xl leading-6 text-gray-700 my-2">
                    <%= camp.title %>
                    </h3>
                    <p class="paragraph-normal text-gray-600">
                    <%= camp.content %>
                    </p>
                    
                    <%= link_to 'Lire plus >>', campagne_path(camp), class:"mt-3 block" %>
                </div>
            </div>
        <% end %>
        
    </div>
</section>